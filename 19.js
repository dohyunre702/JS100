

// 라이브러리(메서드) 사용 (Math.pow(n,m))
// 문자열을 숫자열로 변환하는 persaInt 매서드 사용.

var n = prompt("숫자를 입력하세요", [value]).split("");

console.log(Math.pow(persaInt(n[0],10),persaInt(n[1],10)));


// for문으로 구해볼 수는 없을까?

